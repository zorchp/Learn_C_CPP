#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char const *argv[]) {
    vector<int> v{1, 2, 3};
    int sz = v.size();
    const int n = sz;
    int a[n][n]{};
    return 0;
}
